
variables:
  MY_Var : 'Hello'
steps:
- script: echo This runs in the default shell on any machine
- bash: |
    echo This multiline script always runs in Bash.
    echo Even on Windows machines!
- pwsh: |
    if(Test-Path -Path $(System.DefaultWorkingDirectory)/FirstFile.ps1){
      Write-Output "File path exist"
    }
    else{Write-Output "File Path Doesn't exist"}